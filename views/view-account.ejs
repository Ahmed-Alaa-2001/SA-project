<%- include('parts/header') %>
<%- include('parts/navbar') %>

<div class="container">

    <% if(user.length === 0) { %>
        <p class="alert alert-danger">There is no items</p>
    <% } else { %>
        <table>
            <tr>
                <td>id</td>
                <td> <%= user._id %></td>
            </tr>
            <tr>
                <td>username</td>
                <td><%= user.username %></td>
            </tr>
            <tr>
                <td>email</td>
                <td> <%= user.email%></td>
            </tr>
            <tr>
                <td>Phone</td>
                <td> <%= user.phone%></td>
            </tr>
            <tr>
                <td>Status</td>
                <td> <%= user.Status%></td>
            </tr>
            <tr>
                <td>preferred Payment Method</td>
                <td> <%= user.preferredPaymentMethod%></td>
            </tr>
        <% } %>
        </table>


</div>

<%- include('parts/footer') %>